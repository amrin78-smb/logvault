node_modules/
.next/
.env.local
logs/
*.log
.env

# Read-only DB credentials for local dev (real password lives here, never committed)
.db-readonly.env
temp/
frontend/tsconfig.tsbuildinfo

# packaging archives (build/distribution output, never source)
*.zip

# Claude web UI attachments
.claude-web-images/
.claude-web-files/
