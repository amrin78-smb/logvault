# Read-only PostgreSQL access for Claude Code / local development.
# Copy this file to .db-readonly.env (gitignored) and fill in the real password.
# The real .db-readonly.env exists ONLY on Amrin's local machine, never in git.
DB_READONLY_HOST=192.168.6.111
DB_READONLY_PORT=5432
DB_READONLY_USER=claude_readonly
DB_READONLY_PASS=ask_amrin
